Attention:
   - The first argument should be at the top of the 
      stack (be careful about the order).

implementation:
   ok  - If no parameters are specified, the program 
        must not display anything and give the prompt back

     - Errors include for example: 
         OK - some arguments aren’t integers
         KO - some arguments are bigger than an integer
         KO - or there are duplicates.
     
     - In case of error, it must display "Error" followed 
        by a ’\n’ on the standard error.

     - The first argument should be at the top of the
        stack (be careful about the order).
  

Scripts:
	 -  make Script that receive numbers and excecute 
	 	 push_swap with thats numbers
	 -  

Makefile:
     - Hide objects compilations from ft_printf & libft
     - Show a message something like "ft_printf & libft 
        suscess"

